# TODO

Il backlog non vive più qui. Vive su Jira, progetto `ROBOAI`.

Gli item R1 a R7 che stavano in questo file sono stati riportati uno a uno come
issue sotto l'epic **RoboReason ROS2: planner roadmap**, con lo stesso testo,
gli stessi path di codice e le stesse questioni aperte, più le label di fase
`phase-a`, `phase-b` e `phase-c`:

| Item | Issue | Fase |
|---|---|---|
| R1 zone bounds aware release placement | `ROBOAI-18` | `phase-a` |
| R4 bounding box al posto di position e size | `ROBOAI-19` | `phase-b` |
| R5 riscrittura descrizioni skill e primitive | `ROBOAI-20` | `phase-b` |
| R3 best positioning sulle dimensioni reali | `ROBOAI-21` | `phase-b` |
| R2 skill di push e nudge senza grasp | `ROBOAI-22` | `phase-c` |
| R6 interazione a più chiamate per task | `ROBOAI-23` | `phase-c` |
| R7 geometry fix come skill invocabili | `ROBOAI-24` | `phase-c` |

Il testo originale resta recuperabile dalla git history di questo file.

**Perché il file è stato svuotato.** Un backlog scritto in due posti diverge in
poche settimane, e a quel punto non si sa più quale dei due dica il vero. Jira è
l'unico proprietario dello stato del lavoro, come descritto nella mappa delle
fonti in `CLAUDE.md`.
